function cardUpdateById (req, res) {
    // const cardId = req.params.cardId;
    // const card = req.body;
    // cards = cards.map(el => el.id === cardId ? ({...card, id: el.id}) : el);
    // res.send('Card updated');
    res.send('Card Updated By Id');
}

module.exports = cardUpdateById;